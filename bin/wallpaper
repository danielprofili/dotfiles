#!/bin/bash

FOLDER=~/wallpapers

choice=$(nsxiv -rotb $FOLDER/*)
echo $choice
# wal-fill "$choice"
if [[ -n "$choice" ]]; then
    i3-msg -t run_command restart
    wal -n -i "$choice"
    feh --bg-fill "$choice" 
fi
